server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ricardodamasceno/spring-config-server-repo
          search-paths: user
          default-label: main